import fs from 'fs';
import path from 'path';

const { promises: { mkdir, stat } } = fs; 



class DirectoryCreater {

    create(directory) {
        this.pathParts = directory.split(path.sep);
    }





}




export default function(directory) {
     
}
  
